name: Update Full Repository Zip

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  update-zip:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository with full history
        uses: actions/checkout@v5
        with:
          ref: main
          fetch-depth: 0 # This ensures we get the full git history
          # Do not write the job token into .git/config. This archive is
          # published publicly, and scrubbing the credential afterwards is
          # best-effort at best.
          persist-credentials: false

      - name: Clear git settings
        run: |
          # Remove the [gc] section
          git config --unset gc.auto || true
          
          # Remove the [http "https://github.com/"] section
          git config --unset-all http.https://github.com/.extraheader || true
          
          # Verify the changes
          git config --list

          # Stop file mode changes from being included in the zip
          # git config core.fileMode false

      - name: Create zip file with full git history
        run: |
          # The installer needs git history to self-update, but not the
          # credential store, hooks, or build artefacts.
          rm -f .git/config.lock
          zip -r tts-webui-installer.zip . \
            -x "node_modules/*" \
               "installer_files/*" \
               "*.pyc" \
               "__pycache__/*" \
               ".git/config" \
               ".git/hooks/*" \
               ".git/logs/*"

      - name: Delete existing asset if it exists
        uses: actions/github-script@v6
        continue-on-error: true
        with:
          script: |
            // Get the latest release
            // const { data: release } = await github.rest.repos.getLatestRelease({
            //  owner: context.repo.owner,
            //  repo: context.repo.repo
            // });

            const { data: release } = await github.rest.repos.getReleaseByTag({
              owner: context.repo.owner,
              repo: context.repo.repo,
              tag: 'v0.0.0'
            });

            const assets = release.assets;
            const assetName = 'tts-webui-installer.zip';

            for (const asset of assets) {
              if (asset.name === assetName) {
                console.log(`Deleting existing asset: ${asset.name} (${asset.id})`);
                await github.rest.repos.deleteReleaseAsset({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  asset_id: asset.id
                });
                break;
              }
            }
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload ZIP to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: tts-webui-installer.zip
          tag_name: v0.0.0
          token: ${{ secrets.GITHUB_TOKEN }}